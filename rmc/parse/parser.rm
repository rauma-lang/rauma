use ast.ast;
use lex.stream;

pub fn run_smoke() {
    print("rmc parser smoke:\n");
    ok := lex.stream.parse_demo_with_stream();

    if (ok) {
        ast.ast.print_demo_summary();
    } else {
        print("parser error\n");
    }
}
